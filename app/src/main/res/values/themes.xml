<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for all modes (light and dark) -->
    <style name="Base.Theme.SimpleCalorieCounter2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Common attributes for both light and dark themes -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/shade_of_teal</item>
        <!-- Customize your common theme properties here -->
    </style>

    <!-- Light Theme -->
    <style name="Theme.SimpleCalorieCounter2.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/gray_light</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/shade_of_teal</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <!-- Dark Theme (if you want to support dark mode in the future) -->
    <style name="Theme.SimpleCalorieCounter2.Dark" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <!-- Default theme to apply either light or dark -->
    <style name="Theme.SimpleCalorieCounter2" parent="Base.Theme.SimpleCalorieCounter2">
        <!-- You can switch between light and dark themes here if needed -->
    </style>
</resources>
